<TestCase>
    <CaseName>
        Inserting user with non-existing role
    </CaseName>
    <Tester>
    </Tester>
    <CaseDescription>
        Inserting user with non-existing role
        Expected error:
        Can not add due to referential integrity
    </CaseDescription>
    <SQLScript>
        set @email = 'role@test.com';
	    set @roleId = 4;
	    insert into Users (userId, email, firstname, lastname, password, roleId, majorId) values (UUID(), @email, 'test', 'test', PASSWORD('123abcd'), @roleId, 2);	
    </SQLScript>
    <Result>
    </Result>
</TestCase>
