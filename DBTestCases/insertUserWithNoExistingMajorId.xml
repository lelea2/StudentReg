<TestCase>
    <CaseName>
        Inserting user with non-existing majorId
    </CaseName>
    <Tester>
    </Tester>
    <CaseDescription>
        Insert user with non-existing majorId
        Expected error:
        Can not add due to referential integrity
    </CaseDescription>
    <SQLScript>
        set @email = 'major@test.com';
        set @majorId = 4;
	    insert into Users (userId, email, firstname, lastname, password, roleId, majorId) values (UUID(), @email, 'test', 'test', PASSWORD('123abcd'), 2, @majorId);
    </SQLScript>
    <Result>
    </Result>
</TestCase>
