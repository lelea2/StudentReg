<TestCase>
    <CaseName>
        Update existing user
    </CaseName>
    <Tester>
    </Tester>
    <CaseDescription>
        Create New Student  -- Student has roleId=2, major='computer science'
        Step 1: Get your userId from user table. Eg: top 1 from the list
        Step 2: update user firstname to "updated_"+old_firstname
    </CaseDescription>
    <SQLScript>
        select @userId := userId from Users order by 1 limit 1;
        UPDATE Users set firstname='updated_'+firstname where userId=@userId;
    </SQLScript>
    <Result>
    </Result>
</TestCase>
